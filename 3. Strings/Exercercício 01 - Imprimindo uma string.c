#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(){

    int i, tam = 0;
    char str1[50], str2[50];

    printf("Digite algumas palavras: ");
    gets(str1);

    printf("%s", str1);
}
